# ============================================
# NekoBot 必装依赖
# 安装: pip install -r requirements.txt
# ============================================

# QQ Bot 框架 (可选, 阶段 4: 保留向后兼容)
# ncatbot 仍可作为可选项,通过 .env 中的 BOT_UIN + WS_URI 配置 + NapCat 使用
# 新部署建议使用 QQ Bot 官方机器人(无需 NapCat,通过 QQBOT_APP_ID + QQBOT_APP_SECRET 配置)
ncatbot == 3.8.5

# AI
openai == 1.60.1

# Web 服务
flask>=3.0.0
flask-socketio>=5.3.0
python-socketio>=5.10.0
eventlet>=0.34.0
bcrypt>=4.0.0

# 配置与环境
python-dotenv
pyyaml
Pillow==11.0.0
configparser == 7.2.0
mcp
# 网络请求
httpx>=0.25.0
requests~=2.32.3
websocket-client>=1.8.0
lark-oapi>=1.4.0
# CLI 终端
rich>=13.0.0
pyfiglet>=1.0.0
prompt_toolkit

# JSON 修复
json_repair

# 定时任务
apscheduler

# 加密（Web Push 等需要）
cryptography>=42.0.0,<44

# 二维码生成
segno>=1.5.0

# ---------- 漫画下载 ----------
jmcomic
img2pdf == 0.6.0
pikepdf
imageio==2.36.0
imageio-ffmpeg==0.5.1
beautifulsoup4>=4.12.0

# ---------- 文档解析（知识库） ----------
pdfplumber>=0.10.0
python-docx>=1.0.0
python-pptx>=0.6.0
openpyxl>=3.1.0
PyPDF2>=3.0.0

# ---------- 向量数据库（记忆） ----------
chromadb>=0.4.0

# ---------- 语音识别 ----------
faster-whisper

# ---------- 飞书频道 ----------
lark-oapi>=1.4.0

# ---------- RSS 订阅 ----------
feedparser

# ---------- Web Push 通知 ----------
py-vapid>=1.8,<1.9
pywebpush>=2.0.3

